function plusMinus(arr) {
    let vpos = 0;
    let vneg = 0;
    let vnul = 0;
    for(let i = 0; i < arr.length; i++ ){
        if(arr[i] > 0){
            vpos += 1/arr.length;
        }else if(arr[i] < 0){
            vneg += 1/arr.length;
        }else{
            vnul += 1/arr.length;
        }
    }
    
    console.log(vpos);
    console.log(vneg);
    console.log(vnul);
}
